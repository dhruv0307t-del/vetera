import PetServicesPage from "@/components/pet-services/PetServicesPage";

export const metadata = {
  title: "Pet Services | VetEra",
  description:
    "Explore VetEra's complete suite of pet services — book appointments, find grooming shops, and shop for pet supplies all in one place.",
};

const page = () => {
  return <PetServicesPage />;
};

export default page;
